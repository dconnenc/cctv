export default function Admin() {
  return (
    <section className="page flex-centered">
      {/* <h2 className="hero-title">{'About'}</h2> */}
      <p className="hero-subtitle">{'Admin Portal'}</p>
    </section>
  );
}
